<!doctype html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="referrer" content="same-origin" />
  
  
  <title>TakingNames.io - Domain names for self-hosters</title>

  <link rel="icon" href="/logo.png">

  <style>
    :root {
  --text-color: #222;
  --button-main-color: #555555;
  --background-color: #fff;
  --hover-color: #eee;
  --menu-label-height: 60px;
  --border-radius: 5px;
}

@media (prefers-color-scheme: dark) {
  :root {
    --text-color: #eee;
    --background-color: #222;
  }
}

* {
  background: var(--background-color);
  color: var(--text-color);
}

body {
  font-family: Arial;
  font-size: 1.2em;
  display: flex;
  justify-content: center;
  margin: 0px;
  line-height: 1.5;
}

table, th, td {
  border: 1px solid var(--text-color);
  border-collapse: collapse;
  text-align: center;
  padding: 5px;
}

.content {
  max-width: 640px;
  width: 100%;
  padding: 5px;
}

.navbar {
  font-size: 1.6em;
  font-weight: bold;
  margin-bottom: 20px;
  border: 3px solid var(--text-color);
  border-radius: var(--border-radius);
  padding: 10px;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 10px;
}
.navbar__col {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
.navbar a {
  color: var(--text-color);
}
.navbar__image {
  width: 42px;
}

#search-form {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

#search-label{
  font-size: 1.5em;
}

#search-domain-input{
  font-size: 1.3em;
}

.domain-list-item {
  padding: 10px;
  margin: 10px;
  border: 1px solid var(--text-color);
  border-radius: var(--border-radius);
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-weight: bold;
  gap: 10px;
}
.domain-list-item:hover {
  background: var(--hover-color);
}
.domain-list-item:any-link {
  color: var(--main-color);
  text-decoration: none;
}

.list-item {
  padding: 10px;
  margin: 10px;
  border: 1px solid #333;
  border-radius: var(--border-radius);
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-weight: bold;
  gap: 10px;
}
.list-item:hover {
  background: var(--hover-color);
}

.button {
  padding: .5em 1em;
   
  border: 2px solid var(--button-main-color);
  border-radius: .5em;
  color: #fff;
  background-color: var(--button-main-color);
  text-decoration: none;
  user-select: none;
  cursor: pointer;
  font-family: -system-ui, sans-serif;
  font-size: 1em;
  line-height: 1.2;
  white-space: nowrap;
}

.button:hover {
  color: var(--button-main-color);
  background-color: var(--background-color);
  border: 2px solid var(--button-main-color);
}

.dialog__overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, .5);
  z-index: 1000;
}
.dialog__content {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: var(--background-color);
  padding: 1em;
  border: 3px solid var(--text-color);
  border-radius: var(--border-radius);
  z-index: 1010;
  font-size: 2em;
}

.tn-form {
  display: inline-flex;
  flex-direction: column;
  border: 1px solid var(--text-color);
  border-radius: var(--border-radius);
  padding: 10px;
  gap: 5px;
  align-items: flex-start;
}

.tn-requester-name {
  font-weight: bold;
  font-size: 1.2em;
}

.tn-button-row {
  margin: 20px;
  gap: 10px;
  display: flex;
  justify-content: center;
}

.domain-type-radio {
  margin-top: 20px;
  margin-bottom: 10px;
}
.domain-type-radio-label {
  font-size: 1.2em;
}

#record-host-input {
  max-width: 120px;
  text-align: right;
}

.post-list-item {
  font-size: 1.5em;
}

.tn-lastlogin-btn {
  display: flex;
  align-items: center;
  border: 2px solid var(--text-color);
  border-radius: 5px;
  color: var(--main-color);
  text-decoration: none;
  max-width: 300px;
}

.bold {
  font-weight: bold;
}

.error {
  color: red;
  font-size: 1.2em;
}

  </style>
</head>

<body>
  <div class='content'>
    <nav class='navbar'>
      <a class='navbar__col' href="/">
        <img class='navbar__image' src='/logo.svg' alt='logo letters T and N combined'></img>
      </a>
      <a href="/">Home</a>
      <a href="/blog">Blog</a>
      <a href="https://forum.indiebits.io/c/takingnames-io/8">Support</a>
      
      <a href="/auth/?return_target=%2fblog%2f">Login</a>
      
    </nav>


<h1>Posts</h1>


<div>
  <a href='/blog/instant-subdomains' class='post-list-item'>
    2022-01-07 - Free, instant subdomains without an account
  </a>
</div>

<div>
  <a href='/blog/introducing-takingnames-io' class='post-list-item'>
    2022-01-05 - Introducing TakingNames.io
  </a>
</div>



  </div>
  <script data-goatcounter="https://takingnames-io.goatcounter.com/count"
        async src="//gc.zgo.at/count.js"></script>
</body>
</html>

