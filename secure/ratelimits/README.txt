This directory stores small, transient files used for naive, file-based rate
limiting (one file per client/IP). It must be writable by PHP and MUST NOT be
web-accessible. See /home/<account>/secure/.htaccess.
